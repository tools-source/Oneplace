// API key for the AI assistant.
// Do NOT put real keys here — this file is committed.
// Set the real value in OnePlace/Config/Secrets.local.xcconfig (gitignored),
// which is included below and overrides this placeholder.
ANTHROPIC_API_KEY =

// Optional private overrides live only on your machine.
// Create OnePlace/Config/Secrets.local.xcconfig and keep it untracked.
#include? "Secrets.local.xcconfig"
